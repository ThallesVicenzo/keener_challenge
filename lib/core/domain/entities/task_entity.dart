class TaskEntity {
  final bool isCompleted;
  final String title;
  final String description;

  TaskEntity({
    required this.isCompleted,
    required this.title,
    required this.description,
  });
}
